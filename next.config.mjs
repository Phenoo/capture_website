/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "s.yimg.com",
      "i.insider.com",
      "cdn.sanity.io",
      "images.pexels.com",
    ],
  },
};

export default nextConfig;
